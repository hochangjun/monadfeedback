{
  "name": "monad-feedback-contracts",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "deploy:contract": "hardhat run scripts/deploy.js --network monadTestnet",
    "contract:console": "hardhat console --network monadTestnet",
    "compile": "hardhat compile",
    "test": "hardhat test"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@nomiclabs/hardhat-ethers": "^2.2.3",
    "dotenv": "^16.5.0",
    "ethers": "^5.8.0",
    "hardhat": "^2.24.1"
  }
} 